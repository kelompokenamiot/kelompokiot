import 'package:flutter/material.dart';

class MyLogActivityPage extends StatelessWidget {
  const MyLogActivityPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Log Activity Page'),
    );
  }
}
